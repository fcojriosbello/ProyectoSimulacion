Ficheros a crear:
 · simulacion.cc -> que tendrá tantas funciones como protocolos de acceso al medio
 · Tantos observadores (.cc y .h) como protocolos de acceso al medio

Para no simular todo en el mismo equipo, se ejecutará cada función por separado en cada equipo
(se puede meter un switch case o algo así para hacer esto con el mismo código).

Se estudiarán 2 aplicaciones (VoIP y FTP); por cada aplicaciones habrá 4 gráficas (especificadas en el diario de grupo),
y en cada gráfica 3 curvas (1 por cada protocolo: ALOHA, CSMA-CD y Token Ring).

Habrá 6 simulaciones distintas (2 aplicaciones por 3 protocolos), aunque serán muy parecidas.
